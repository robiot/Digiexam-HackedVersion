/* globals svgEditor */

/*
 * See defaultConfig, config-sample.js and defaultExtensions in svg-editor.js for a list
 * of possible configuration settings.
 *
 * See svg-editor.js for documentation on using setConfig().
 */

svgEditor.setConfig({
	extensions: [],
	canvas_expansion: 1,
	initTool: 'fhpath',
	showRulers: false,
});